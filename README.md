# Aurora Login

A small, responsive login page project built with plain HTML, CSS, and JavaScript.

## Run locally

Use a local static server from the project folder:

```powershell
python -m http.server 3000
```

Then open `http://localhost:3000` in your browser.
